apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.app.service }}
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: {{ .Release.Namespace }}
  ports:
  - name: alias8080
    port: 8080
    targetPort: 8080
